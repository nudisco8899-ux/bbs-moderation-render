services:
  - type: cron
    name: bbs-moderation-checker
    runtime: node
    buildCommand: npm install --production
    startCommand: node /opt/render/project/index.js
    schedule: "0 * * * *"
    env:
      - key: NODE_ENV
        value: production
